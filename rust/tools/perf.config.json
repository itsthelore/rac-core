{
  "engines": {
    "oracle": {
      "bin": "/home/user/rac-core/.venv-oracle/bin/rac",
      "version": ["--version"],
      "single_validate": ["validate", "{file}", "--no-cache"],
      "walk": ["validate", "{dir}", "--no-cache"]
    },
    "rust": {
      "bin": "../target/release/decided",
      "version": ["--version"],
      "single_validate": ["validate", "{file}"],
      "walk": ["validate", "{dir}"],
      "find": ["find", "{query}", "{dir}", "--json"]
    }
  },
  "corpora": {
    "1000": ".corpora/c1000",
    "5000": ".corpora/c5000",
    "10000": ".corpora/c10000",
    "100000": ".corpora/c100000"
  },
  "runs": {
    "default": 7,
    "10000": 5,
    "100000": 3
  }
}
