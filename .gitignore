# macOS
.DS_Store

# Virtual environments
.venv/
.venv-oracle/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/

# Coverage data (pytest --cov; reported in CI, never committed)
.coverage
.coverage.*
htmlcov/

# MkDocs build output (deployed from CI, never committed)
site/

# Native engine binaries bundled into platform wheels at build time
# (native-engine cutover); never committed — the package keeps only __init__.py.
src/rac/bin/rac
src/rac/bin/rac-mcp
src/rac/bin/*.exe
