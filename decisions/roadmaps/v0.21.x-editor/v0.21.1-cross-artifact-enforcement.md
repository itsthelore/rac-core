---
schema_version: 1
id: RAC-KV6FFVFKMB6T
type: roadmap
tags: [sdk, typescript, editor, enforcement]
---
# RAC v0.21.1 — Cross-Artifact Enforcement in the Editor

## Status

Achieved

## Context

The v0.21.0 extension validates each artifact in isolation — structural rules on
the file in front of you. RAC's actual value is cross-artifact: "enforcement is
the product" (ADR-049). A reference to a missing decision, or to one that has
been **retired** (Superseded/Deprecated, ADR-051), is exactly what RAC exists to
catch, and the place a human needs to see it is where they typed it.

This release makes the extension surface those violations in the editor, drawn
distinctly from per-file structural issues. It is the feature that makes the
extension RAC rather than a generic Markdown linter
(`rac-cross-artifact-enforcement`).

## Outcomes

- A declared reference that does not resolve is flagged at the reference, not
  only in a CLI run.
- A reference to a retired artifact is flagged distinctly from one that does not
  resolve at all.
- The findings match `rac relationships --validate` / `rac review`, because the
  extension runs them — it does not re-derive relationships (ADR-063).

## Initiatives

### Initiative 1 — Relationship diagnostics at the reference site

Run `rac relationships --validate` over the workspace and map each issue
(`source_path`, `relationship`, `target`, `code`) to a diagnostic. Relationship
issues carry no line, so the extension locates the `target` token within the
declared relationship section of the source artifact and anchors the diagnostic
there (falling back to the section heading, then the file head). Because
relationship validation reads files from disk, it reflects the *saved* corpus —
so it refreshes on save and on activation, not on unsaved buffer edits (which
`rac relationships` cannot see). Diagnostics live in their own collection,
separate from the live per-file validation diagnostics.

### Initiative 2 — Retired-target awareness

The engine already distinguishes a reference to a retired
(superseded/deprecated, ADR-051) artifact: `rac relationships --validate` emits
`relationship-target-superseded` as its own `code`, separate from
`relationship-target-not-found`. The extension maps the two to distinct messages
and severities — a retired reference is an advisory warning ("you are pointing at
superseded thinking"), an unresolved one an error ("this target does not exist")
— so no separate status lookup is needed.

## Constraints

- Thin client only (ADR-063): findings come from `rac relationships --validate` /
  `rac review` / `rac resolve`; the extension computes no relationship graph.
- Additive JSON consumption (ADR-007).
- Must stay responsive: enforcement runs are debounced and scoped, not
  per-keystroke over the whole corpus.

## Non-Goals

- Auto-fixing references (a quick-fix to search for the intended target is
  v0.21.2 territory).
- A relationship graph view (v0.21.5).
- Changing how `rac` computes relationships or statuses.

## Implementation Contract

- Diagnostics come from `rac relationships --validate` and are anchored to the
  target token inside the source artifact's relationship section, with the
  section heading and then the file head as fallbacks.
- `relationship-target-not-found` maps to an error and
  `relationship-target-superseded` to a warning, so retired references read
  distinctly from unresolved ones; the other relationship codes (ambiguous,
  type-mismatch, self-reference, cycle, edge-unsupported) map to sensible
  severities.
- Relationship diagnostics live in a separate collection from per-file
  validation, and refresh on save/activation (debounced), since relationship
  validation reads disk and cannot see unsaved edits.

## Success Measures

- A reference to a missing artifact shows a diagnostic at that reference,
  consistent with `rac relationships --validate`.
- A reference to a Superseded/Deprecated artifact shows the distinct
  retired-target diagnostic.
- Fixing the reference clears the diagnostic on the next debounced run.

## Risks

- Relationship issues lack a line/anchor, so localization relies on finding the
  target token in the relationship section. Mitigated by that section-scoped
  search; if brittle, a small additive `--json` line/anchor on
  `rac relationships` is a separate, scoped `rac` change.
- Corpus-wide relationship validation cost on large repos. Mitigated by
  debouncing, caching (v0.21.6), and running on save rather than every keystroke.

## Assumptions

- `rac relationships --validate` and `rac review`, plus status from
  `rac resolve` / `rac export`, expose enough to identify broken and retired
  references.

## Related Decisions

- adr-007
- adr-049
- adr-051
- adr-063

## Related Requirements

- rac-cross-artifact-enforcement

## Related Roadmaps

- v0.21.0-typescript-sdk-and-extension
- v0.21.2-authoring-ergonomics
